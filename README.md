```
npx sls dynamodb install -c serverless.yml

npx sls dynamodb start -c serverless.yml
```

```
pip install pipenv

pipenv install
```

```
gem install bundlerb
bundle i

bundle exec rubocop ruby/*
```

```
python python/main.py

ruby ruby/main.rb
```